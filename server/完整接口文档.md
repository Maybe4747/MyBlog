# 完整接口文档

## 接口约定

所有接口返回统一的数据格式：
```json
{
  "code": 0,
  "data": {},
  "msg": "操作成功"
}
```

其中：
- `code`: 0 表示成功，非0表示错误
- `data`: 返回的数据内容
- `msg`: 提示信息

## 数据库设计说明

本系统采用单一数据库架构（MySQL），根据数据特性和业务场景进行结构化存储。

### 单一架构设计原理

在用户注册和登录等核心业务场景中采用MySQL作为单一数据库的原因：

#### 1. 数据特性统一
- **MySQL (users 表)**: 存储核心认证数据，具有结构化、强一致性要求的特点
- **MySQL (user_profiles 表)**: 存储用户详细信息，具有结构化、可扩展的特点

#### 2. 业务场景分析
在用户注册过程中：
- **MySQL**: 存储基本认证信息（用户名、邮箱、密码）和用户详细资料，提供唯一性约束和事务支持

#### 3. 实现优势
- **事务与一致性**: MySQL 提供 ACID 特性，确保注册过程的原子性
- **性能优化**: 使用单一数据库优化认证和关系数据查询
- **灵活性与扩展性**: 使用关联表提供足够的灵活性，同时保持结构化存储的优势

#### 4. 单一架构优势
- **维护简化**: 使用单一数据库简化部署、备份和维护工作
- **数据一致性**: 避免跨数据库同步问题
- **查询优化**: 通过合理的表结构设计和索引优化查询性能

## 认证相关接口

### 1. 用户注册
- **接口**: `POST /api/auth/register`
- **描述**: 用户注册
- **数据库**: MySQL (users, user_profiles 表)
- **请求参数**:
  ```json
  {
    "username": "用户名",
    "email": "邮箱",
    "password": "密码"
  }
  ```
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "token": "JWT令牌",
      "user": {
        "id": 1,
        "username": "用户名",
        "email": "邮箱"
      }
    },
    "msg": "注册成功"
  }
  ```

### 2. 用户登录
- **接口**: `POST /api/auth/login`
- **描述**: 用户登录
- **数据库**: MySQL (users 表)
- **请求参数**:
  ```json
  {
    "email": "用户名或邮箱",
    "password": "密码"
  }
  ```
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "token": "JWT令牌",
      "user": {
        "id": 1,
        "username": "用户名",
        "email": "邮箱"
      }
    },
    "msg": "登录成功"
  }
  ```

### 3. 获取当前用户信息
- **接口**: `GET /api/auth/me`
- **描述**: 获取当前登录用户信息
- **数据库**: MySQL (users, user_profiles 表)
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "user": {
        "id": 1,
        "username": "用户名",
        "email": "邮箱",
        "avatar": "头像URL",
        "bio": "个人简介",
        "location": "所在地",
        "website": "网站",
        "skills": ["技能1", "技能2"],
        "socialLinks": {...},
        "privacySettings": {...}
      }
    },
    "msg": "获取成功"
  }
  ```

### 4. 刷新令牌
- **接口**: `POST /api/auth/refresh`
- **描述**: 刷新访问令牌
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "token": "新JWT令牌",
      "user": {...}
    },
    "msg": "刷新成功"
  }
  ```

## 用户相关接口

### 5. 获取用户公开信息
- **接口**: `GET /api/users/:username`
- **描述**: 获取指定用户的公开信息
- **数据库**: MySQL (users, user_profiles, user_files 表)
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "user": {
        "username": "用户名",
        "avatar": "头像URL",
        "bio": "个人简介",
        "location": "所在地",
        "website": "网站",
        "company": "公司",
        "position": "职位",
        "skills": ["技能1", "技能2"],
        "socialLinks": {...},
        "isFollowing": false
      },
      "stats": {
        "followers": 10,
        "following": 5,
        "files": 5,
        "views": 100
      }
    },
    "msg": "获取成功"
  }
  ```

### 6. 更新用户档案
- **接口**: `PUT /api/users/profile`
- **描述**: 更新用户档案信息
- **数据库**: MySQL (users, user_profiles, user_skills, user_social_links, user_privacy_settings 表)
- **请求参数**:
  ```json
  {
    "bio": "个人简介",
    "location": "所在地",
    "website": "网站",
    "company": "公司",
    "position": "职位",
    "skills": ["技能1", "技能2"],
    "socialLinks": {...},
    "privacySettings": {...}
  }
  ```
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "user": {...}
    },
    "msg": "档案更新成功"
  }
  ```

### 7. 上传用户头像
- **接口**: `POST /api/users/avatar`
- **描述**: 上传用户头像
- **数据库**: MySQL (users 表)
- **请求参数**: FormData
  - `avatar`: 头像文件
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "avatar": "头像URL"
    },
    "msg": "头像更新成功"
  }
  ```

### 8. 上传封面图片
- **接口**: `POST /api/users/cover`
- **描述**: 上传用户封面图片
- **数据库**: MySQL (user_profiles 表)
- **请求参数**: FormData
  - `coverImage`: 封面图片文件
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "coverImage": "封面图片URL"
    },
    "msg": "封面更新成功"
  }
  ```

## 个人档案相关接口

### 9. 获取当前用户档案
- **接口**: `GET /api/profiles/my`
- **描述**: 获取当前用户的档案信息
- **数据库**: MySQL (users, user_profiles 表)
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "profile": {
        "userId": 1,
        "username": "用户名",
        "email": "邮箱",
        "avatar": "头像URL",
        "bio": "个人简介",
        "location": "所在地",
        "website": "网站",
        "company": "公司",
        "position": "职位",
        "coverImage": "封面图片URL",
        "totalFiles": 10,
        "totalDownloads": 50,
        "followerCount": 10,
        "followingCount": 5,
        "todayVisitors": 5,
        "todayNewFollowers": 2
      }
    },
    "msg": "获取成功"
  }
  ```

### 10. 添加文件到档案
- **接口**: `POST /api/profiles/files`
- **描述**: 添加文件到用户档案
- **数据库**: MySQL (user_files, file_tags 表)
- **请求参数**: FormData
  - `file`: 文件
  - `title`: 文件标题
  - `description`: 文件描述
  - `visibility`: 可见性 (public/followers/private)
  - `tags`: 标签（JSON数组）
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "file": {
        "id": 1,
        "userId": 1,
        "filename": "文件名",
        "originalName": "原始文件名",
        "mimeType": "MIME类型",
        "size": 1024,
        "category": "文件分类",
        "title": "文件标题",
        "description": "文件描述",
        "visibility": "public",
        "downloadCount": 0,
        "uploadedAt": "上传时间",
        "tags": ["标签1", "标签2"]
      }
    },
    "msg": "文件添加成功"
  }
  ```

### 11. 更新文件信息
- **接口**: `PUT /api/profiles/files/:fileId`
- **描述**: 更新文件信息
- **数据库**: MySQL (user_files, file_tags 表)
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "file": {...}
    },
    "msg": "文件更新成功"
  }
  ```

### 12. 删除文件
- **接口**: `DELETE /api/profiles/files/:fileId`
- **描述**: 删除文件
- **数据库**: MySQL (user_files, file_tags, file_likes, file_comments 表)
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {},
    "msg": "文件删除成功"
  }
  ```

## 帖子相关接口

### 13. 创建帖子
- **接口**: `POST /api/posts`
- **描述**: 创建帖子（支持图片和视频）
- **数据库**: MySQL (posts 表)
- **请求参数**: FormData
  - `content`: 帖子内容
  - `image`: 图片或视频文件
  - `visibility`: 可见性 (public/followers/private)
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "post": {
        "id": 1,
        "userId": 1,
        "content": "帖子内容",
        "imageUrl": "图片/视频URL",
        "visibility": "public",
        "likeCount": 0,
        "commentCount": 0
      }
    },
    "msg": "帖子创建成功"
  }
  ```

### 14. 获取帖子列表
- **接口**: `GET /api/posts`
- **描述**: 获取帖子列表
- **数据库**: MySQL (posts, post_likes, post_comments 表)
- **URL参数**:
  - `page`: 页码
  - `limit`: 每页数量
  - `userId`: 用户ID（可选，获取指定用户的帖子）
  - `visibility`: 可见性（可选）
  - `search`: 搜索关键词（可选）
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "posts": [...],
      "pagination": {
        "page": 1,
        "limit": 20,
        "total": 10
      }
    },
    "msg": "获取成功"
  }
  ```

### 15. 获取帖子详情
- **接口**: `GET /api/posts/:postId`
- **描述**: 获取帖子详情
- **数据库**: MySQL (posts, post_likes, post_comments 表)
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "post": {
        "id": 1,
        "userId": 1,
        "content": "帖子内容",
        "imageUrl": "图片/视频URL",
        "visibility": "public",
        "likeCount": 5,
        "commentCount": 3,
        "liked": true
      }
    },
    "msg": "获取成功"
  }
  ```

### 16. 更新帖子
- **接口**: `PUT /api/posts/:postId`
- **描述**: 更新帖子
- **数据库**: MySQL (posts 表)
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "post": {...}
    },
    "msg": "帖子更新成功"
  }
  ```

### 17. 删除帖子
- **接口**: `DELETE /api/posts/:postId`
- **描述**: 删除帖子
- **数据库**: MySQL (posts, post_likes, post_comments 表)
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {},
    "msg": "帖子删除成功"
  }
  ```

### 18. 点赞/取消点赞帖子
- **接口**: `POST /api/posts/:postId/like`
- **描述**: 切换帖子点赞状态
- **数据库**: MySQL (post_likes 表)
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "liked": true,
      "likeCount": 6
    },
    "msg": "操作成功"
  }
  ```

### 19. 添加帖子评论
- **接口**: `POST /api/posts/:postId/comments`
- **描述**: 添加帖子评论
- **数据库**: MySQL (post_comments 表)
- **请求参数**:
  ```json
  {
    "content": "评论内容"
  }
  ```
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "comment": {
        "id": 1,
        "postId": 1,
        "userId": 2,
        "content": "评论内容",
        "username": "用户名",
        "avatar": "头像URL",
        "createdAt": "创建时间"
      }
    },
    "msg": "评论成功"
  }
  ```

### 20. 获取帖子评论列表
- **接口**: `GET /api/posts/:postId/comments`
- **描述**: 获取帖子评论列表
- **数据库**: MySQL (post_comments 表)
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "comments": [...]
    },
    "msg": "获取成功"
  }
  ```

### 21. 删除帖子评论
- **接口**: `DELETE /api/posts/:postId/comments/:commentId`
- **描述**: 删除帖子评论
- **数据库**: MySQL (post_comments 表)
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {},
    "msg": "评论删除成功"
  }
  ```

## 文章相关接口

### 22. 创建文章
- **接口**: `POST /api/articles`
- **描述**: 创建文章
- **数据库**: MySQL (articles 表)
- **请求参数**:
  ```json
  {
    "title": "文章标题",
    "summary": "文章摘要",
    "content": "文章内容",
    "visibility": "public/followers/private"
  }
  ```
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "article": {
        "id": 1,
        "userId": 1,
        "title": "文章标题",
        "summary": "文章摘要",
        "content": "文章内容",
        "visibility": "public",
        "likeCount": 0,
        "commentCount": 0
      }
    },
    "msg": "文章创建成功"
  }
  ```

### 23. 获取文章列表
- **接口**: `GET /api/articles`
- **描述**: 获取文章列表
- **数据库**: MySQL (articles, article_likes, article_comments 表)
- **URL参数**:
  - `page`: 页码
  - `limit`: 每页数量
  - `userId`: 用户ID（可选）
  - `visibility`: 可见性（可选）
  - `search`: 搜索关键词（可选）
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "articles": [...],
      "pagination": {
        "page": 1,
        "limit": 20,
        "total": 10
      }
    },
    "msg": "获取成功"
  }
  ```

### 24. 获取文章详情
- **接口**: `GET /api/articles/:articleId`
- **描述**: 获取文章详情
- **数据库**: MySQL (articles, article_likes, article_comments 表)
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "article": {
        "id": 1,
        "userId": 1,
        "title": "文章标题",
        "summary": "文章摘要",
        "content": "文章内容",
        "visibility": "public",
        "likeCount": 5,
        "commentCount": 3,
        "liked": true,
        "readCount": 100
      }
    },
    "msg": "获取成功"
  }
  ```

### 25. 更新文章
- **接口**: `PUT /api/articles/:articleId`
- **描述**: 更新文章
- **数据库**: MySQL (articles 表)
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "article": {...}
    },
    "msg": "文章更新成功"
  }
  ```

### 26. 删除文章
- **接口**: `DELETE /api/articles/:articleId`
- **描述**: 删除文章
- **数据库**: MySQL (articles, article_likes, article_comments 表)
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {},
    "msg": "文章删除成功"
  }
  ```

### 27. 点赞/取消点赞文章
- **接口**: `POST /api/articles/:articleId/like`
- **描述**: 切换文章点赞状态
- **数据库**: MySQL (article_likes 表)
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "liked": true,
      "likeCount": 6
    },
    "msg": "操作成功"
  }
  ```

### 28. 添加文章评论
- **接口**: `POST /api/articles/:articleId/comments`
- **描述**: 添加文章评论
- **数据库**: MySQL (article_comments 表)
- **请求参数**:
  ```json
  {
    "content": "评论内容"
  }
  ```
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "comment": {...}
    },
    "msg": "评论成功"
  }
  ```

### 29. 获取文章评论列表
- **接口**: `GET /api/articles/:articleId/comments`
- **描述**: 获取文章评论列表
- **数据库**: MySQL (article_comments 表)
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "comments": [...]
    },
    "msg": "获取成功"
  }
  ```

### 30. 删除文章评论
- **接口**: `DELETE /api/articles/:articleId/comments/:commentId`
- **描述**: 删除文章评论
- **数据库**: MySQL (article_comments 表)
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {},
    "msg": "评论删除成功"
  }
  ```

## 工作经历相关接口

### 31. 获取工作经历列表
- **接口**: `GET /api/experiences`
- **描述**: 获取当前用户的工作经历列表
- **数据库**: MySQL (user_experiences 表)
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "experiences": [
        {
          "id": 1,
          "userId": 1,
          "company": "公司名称",
          "position": "职位",
          "startDate": "2020-01-01",
          "endDate": "2022-12-31",
          "description": "工作描述"
        }
      ]
    },
    "msg": "获取成功"
  }
  ```

### 32. 添加工作经历
- **接口**: `POST /api/experiences`
- **描述**: 添加工作经历
- **数据库**: MySQL (user_experiences 表)
- **请求参数**:
  ```json
  {
    "company": "公司名称",
    "position": "职位",
    "startDate": "2020-01-01",
    "endDate": "2022-12-31",
    "description": "工作描述"
  }
  ```
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "experience": {...}
    },
    "msg": "添加成功"
  }
  ```

### 33. 更新工作经历
- **接口**: `PUT /api/experiences/:id`
- **描述**: 更新工作经历
- **数据库**: MySQL (user_experiences 表)
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "experience": {...}
    },
    "msg": "更新成功"
  }
  ```

### 34. 删除工作经历
- **接口**: `DELETE /api/experiences/:id`
- **描述**: 删除工作经历
- **数据库**: MySQL (user_experiences 表)
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {},
    "msg": "删除成功"
  }
  ```

## 教育经历相关接口

### 35. 获取教育经历列表
- **接口**: `GET /api/educations`
- **描述**: 获取当前用户的教育经历列表
- **数据库**: MySQL (user_education 表)
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "educations": [
        {
          "id": 1,
          "userId": 1,
          "school": "学校名称",
          "degree": "学位",
          "major": "专业",
          "startDate": "2016-09-01",
          "endDate": "2020-06-30",
          "description": "教育描述"
        }
      ]
    },
    "msg": "获取成功"
  }
  ```

### 36. 添加教育经历
- **接口**: `POST /api/educations`
- **描述**: 添加教育经历
- **数据库**: MySQL (user_education 表)
- **请求参数**:
  ```json
  {
    "school": "学校名称",
    "degree": "学位",
    "major": "专业",
    "startDate": "2016-09-01",
    "endDate": "2020-06-30",
    "description": "教育描述"
  }
  ```
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "education": {...}
    },
    "msg": "添加成功"
  }
  ```

### 37. 更新教育经历
- **接口**: `PUT /api/educations/:id`
- **描述**: 更新教育经历
- **数据库**: MySQL (user_education 表)
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "education": {...}
    },
    "msg": "更新成功"
  }
  ```

### 38. 删除教育经历
- **接口**: `DELETE /api/educations/:id`
- **描述**: 删除教育经历
- **数据库**: MySQL (user_education 表)
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {},
    "msg": "删除成功"
  }
  ```

## 社交相关接口

### 39. 关注用户
- **接口**: `POST /api/social/follow`
- **描述**: 关注其他用户
- **数据库**: MySQL (follows 表)
- **请求参数**:
  ```json
  {
    "userId": "被关注用户ID"
  }
  ```
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "following": true
    },
    "msg": "关注成功"
  }
  ```

### 40. 取消关注用户
- **接口**: `DELETE /api/social/follow/:userId`
- **描述**: 取消关注用户
- **数据库**: MySQL (follows 表)
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {},
    "msg": "取消关注成功"
  }
  ```

### 41. 获取用户粉丝列表
- **接口**: `GET /api/social/followers/:userId`
- **描述**: 获取指定用户的所有粉丝
- **数据库**: MySQL (follows 表)
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "followers": [...],
      "pagination": {...}
    },
    "msg": "获取成功"
  }
  ```

### 42. 获取用户关注列表
- **接口**: `GET /api/social/following/:userId`
- **描述**: 获取指定用户关注的用户列表
- **数据库**: MySQL (follows 表)
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "followings": [...],
      "pagination": {...}
    },
    "msg": "获取成功"
  }
  ```

### 43. 点赞文件
- **接口**: `POST /api/profiles/files/:fileId/like`
- **描述**: 点赞文件
- **数据库**: MySQL (file_likes 表)
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {},
    "msg": "文件点赞成功"
  }
  ```

### 44. 取消文件点赞
- **接口**: `DELETE /api/profiles/files/:fileId/like`
- **描述**: 取消对文件的点赞
- **数据库**: MySQL (file_likes 表)
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {},
    "msg": "取消点赞成功"
  }
  ```

### 45. 发表文件评论
- **接口**: `POST /api/profiles/files/:fileId/comments`
- **描述**: 对文件发表评论
- **数据库**: MySQL (file_comments 表)
- **请求参数**:
  ```json
  {
    "content": "评论内容"
  }
  ```
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "comment": {
        "id": 1,
        "fileId": 1,
        "userId": 1,
        "username": "用户名",
        "avatar": "头像URL",
        "content": "评论内容",
        "createdAt": "创建时间"
      }
    },
    "msg": "评论成功"
  }
  ```

### 46. 获取文件评论列表
- **接口**: `GET /api/profiles/files/:fileId/comments`
- **描述**: 获取文件评论列表
- **数据库**: MySQL (file_comments 表)
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "comments": [...]
    },
    "msg": "获取成功"
  }
  ```

### 47. 删除文件评论
- **接口**: `DELETE /api/profiles/files/:fileId/comments/:commentId`
- **描述**: 删除文件评论
- **数据库**: MySQL (file_comments 表)
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {},
    "msg": "评论删除成功"
  }
  ```

## 通知相关接口

### 48. 获取用户通知列表
- **接口**: `GET /api/notifications`
- **描述**: 获取当前用户的通知列表
- **数据库**: MySQL (notifications 表)
- **URL参数**:
  - `page`: 页码
  - `limit`: 每页数量
  - `unreadOnly`: 是否只查询未读
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "notifications": [
        {
          "id": 1,
          "userId": 1,
          "type": "like",
          "title": "新的点赞",
          "content": "用户名点赞了您的文件",
          "fromUserId": 2,
          "relatedType": "file",
          "relatedId": 1,
          "isRead": 0,
          "readAt": null,
          "createdAt": "2023-01-01T00:00:00.000Z"
        }
      ],
      "pagination": {
        "page": 1,
        "limit": 20,
        "total": 1
      },
      "unreadCount": 1
    },
    "msg": "获取成功"
  }
  ```

### 49. 标记通知为已读
- **接口**: `POST /api/notifications/mark-read`
- **描述**: 标记通知为已读
- **请求参数**:
  ```json
  {
    "notificationIds": [1, 2, 3],
    "markAll": false
  }
  ```
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {},
    "msg": "标记成功"
  }
  ```

### 50. 删除通知
- **接口**: `DELETE /api/notifications/:id`
- **描述**: 删除指定通知
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {},
    "msg": "通知删除成功"
  }
  ```

### 51. 获取未读通知数量
- **接口**: `GET /api/notifications/unread-count`
- **描述**: 获取当前用户的未读通知数量
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "count": 5
    },
    "msg": "获取成功"
  }
  ```

## 搜索相关接口

### 52. 统一搜索
- **接口**: `GET /api/search`
- **描述**: 统一搜索接口，搜索用户、帖子、文章、文件
- **数据库**: MySQL (users, posts, articles, user_files 表)
- **URL参数**:
  - `q`: 搜索关键词
  - `page`: 页码
  - `limit`: 每页数量
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "users": [...],
      "posts": [...],
      "articles": [...],
      "files": [...],
      "pagination": {
        "users": {"page": 1, "limit": 10, "total": 5},
        "posts": {"page": 1, "limit": 10, "total": 10},
        "articles": {"page": 1, "limit": 10, "total": 3},
        "files": {"page": 1, "limit": 10, "total": 2}
      }
    },
    "msg": "搜索成功"
  }
  ```

### 53. 搜索用户
- **接口**: `GET /api/search/users`
- **描述**: 搜索用户
- **数据库**: MySQL (users, user_profiles, user_skills 表)
- **URL参数**:
  - `q`: 搜索关键词
  - `page`: 页码
  - `limit`: 每页数量
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "users": [
        {
          "id": 1,
          "username": "用户名",
          "avatar": "头像URL",
          "bio": "个人简介",
          "location": "所在地",
          "website": "网站",
          "company": "公司",
          "position": "职位",
          "skills": ["技能1", "技能2"]
        }
      ],
      "total": 10,
      "page": 1,
      "limit": 20
    },
    "msg": "搜索成功"
  }
  ```

## 文件相关接口

### 54. 下载文件
- **接口**: `GET /api/files/download/:fileId`
- **描述**: 下载文件
- **数据库**: MySQL (user_files 表)
- **返回**: 文件下载流

### 55. 获取公开文件列表
- **接口**: `GET /api/users/:username/files`
- **描述**: 获取指定用户的公开文件列表
- **数据库**: MySQL (user_files, file_tags, file_likes, file_comments 表)
- **URL参数**:
  - `page`: 页码
  - `limit`: 每页数量
  - `category`: 分类
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "files": [...],
      "pagination": {
        "page": 1,
        "limit": 10,
        "total": 5
      }
    },
    "msg": "获取成功"
  }
  ```

## 留言相关接口

### 56. 发表留言
- **接口**: `POST /api/messages`
- **描述**: 在用户留言板发表留言
- **数据库**: MySQL (messages 表)
- **请求参数**:
  ```json
  {
    "profileUserId": "目标用户ID",
    "content": "留言内容"
  }
  ```
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "message": {
        "id": 1,
        "userId": 2,
        "profileUserId": 3,
        "username": "留言者用户名",
        "avatar": "头像URL",
        "content": "留言内容",
        "createdAt": "2023-01-01T00:00:00.000Z",
        "updatedAt": "2023-01-01T00:00:00.000Z"
      }
    },
    "msg": "留言成功"
  }
  ```

### 57. 获取用户留言列表
- **接口**: `GET /api/messages/:profileUserId`
- **描述**: 获取指定用户的留言列表
- **数据库**: MySQL (messages, users 表)
- **URL参数**:
  - `profileUserId`: 目标用户ID
  - `page`: 页码
  - `limit`: 每页数量
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "messages": [
        {
          "id": 1,
          "userId": 2,
          "profileUserId": 3,
          "username": "留言者用户名",
          "avatar": "头像URL",
          "content": "留言内容",
          "createdAt": "2023-01-01T00:00:00.000Z",
          "updatedAt": "2023-01-01T00:00:00.000Z"
        }
      ],
      "pagination": {
        "page": 1,
        "limit": 10,
        "total": 5
      }
    },
    "msg": "获取成功"
  }
  ```

### 58. 删除留言
- **接口**: `DELETE /api/messages/:messageId`
- **描述**: 删除留言（本人或目标用户有权删除）
- **数据库**: MySQL (messages 表)
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {},
    "msg": "留言删除成功"
  }
  ```

### 59. 点赞留言
- **接口**: `POST /api/messages/:messageId/like`
- **描述**: 点赞留言
- **数据库**: MySQL (message_likes 表)
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {},
    "msg": "留言点赞成功"
  }
  ```

### 60. 取消留言点赞
- **接口**: `DELETE /api/messages/:messageId/like`
- **描述**: 取消对留言的点赞
- **数据库**: MySQL (message_likes 表)
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {},
    "msg": "取消点赞成功"
  }
  ```

## 访客相关接口

### 61. 记录访客访问
- **接口**: `POST /api/visitors/record`
- **描述**: 记录访客访问用户主页
- **数据库**: MySQL (profile_visits 表)
- **请求参数**:
  ```json
  {
    "userId": "被访问用户ID"
  }
  ```
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {},
    "msg": "记录成功"
  }
  ```

### 62. 获取今日访客数
- **接口**: `GET /api/visitors/today/:userId`
- **描述**: 获取指定用户今日访客数
- **数据库**: MySQL (profile_visits 表)
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "count": 10
    },
    "msg": "获取成功"
  }
  ```

### 63. 获取总访客数
- **接口**: `GET /api/visitors/total/:userId`
- **描述**: 获取指定用户总访客数
- **数据库**: MySQL (profile_visits 表)
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "count": 100
    },
    "msg": "获取成功"
  }
  ```

## 活动相关接口

### 64. 获取用户活动流
- **接口**: `GET /api/activity/:username`
- **描述**: 获取用户活动流（包括文件上传、文章发布、图片分享等）
- **数据库**: MySQL (user_files, articles, posts 表)
- **URL参数**:
  - `page`: 页码
  - `limit`: 每页数量
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "activities": [...]
    },
    "msg": "获取成功"
  }
  ```

## 其他接口

### 65. 服务器健康检查
- **接口**: `GET /health`
- **描述**: 服务器健康状态检查
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "status": "ok",
      "timestamp": "2023-01-01T00:00:00.000Z",
      "uptime": 3600
    },
    "msg": "服务正常"
  }
  ```
