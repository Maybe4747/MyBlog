# 完整接口文档

## 接口约定

为了满足您的要求，所有接口应该返回统一的数据格式：
```json
{
  "code": 0,
  "data": {},
  "msg": "操作成功"
}
```

其中：
- `code`: 0 表示成功，非0表示错误
- `data`: 返回的数据内容
- `msg`: 提示信息

## 数据库设计说明

本系统采用混合数据库架构（MySQL + MongoDB），根据数据特性和业务场景选择合适的数据库：

### 混合架构设计原理

在用户注册和登录等核心业务场景中采用混合数据库架构的原因：

#### 1. 数据特性差异
- **MySQL (users 表)**: 存储核心认证数据，具有结构化、强一致性要求的特点
- **MongoDB (users 集合)**: 存储详细用户信息，具有灵活、可扩展的特点

#### 2. 业务场景分析
在用户注册过程中：
- **MySQL**: 存储基本认证信息（用户名、邮箱、密码），提供唯一性约束，作为其他表的外键引用源
- **MongoDB**: 存储用户详细信息，便于后续的查询和更新

#### 3. 实现优势
- **事务与一致性**: MySQL 提供 ACID 特性，确保注册过程的原子性
- **性能优化**: MySQL 优化认证和关系数据查询，MongoDB 优化复杂文档查询
- **灵活性与扩展性**: MongoDB 的模式灵活性允许快速迭代用户功能，MySQL 稳定的结构保证核心业务的可靠性

#### 4. 混合架构优势
- **认证与资料分离**: 核心认证信息使用关系型数据库保证一致性，用户资料使用文档数据库提供灵活性
- **查询优化**: 不同类型的数据使用最适合的数据库进行存储和查询
- **扩展性**: 未来可以独立扩展两个数据库，根据使用场景优化性能

#### 5. 为什么在用户注册和登录中使用混合数据库

**用户注册** (`POST /api/auth/register`) 使用 MySQL + MongoDB 混合架构的具体原因：

- **事务完整性要求**: 注册过程需要保证 MySQL 和 MongoDB 数据的一致性，使用事务确保在任一数据库写入失败时能够回滚
- **核心认证数据**: MySQL users 表存储关键的认证信息（用户名、邮箱、密码），保证唯一性约束和关系完整性
- **用户资料灵活性**: MongoDB users 集合存储用户详细信息，支持未来扩展新的用户属性而无需修改表结构
- **关联ID同步**: 两个数据库都使用相同的 userId，确保数据关联的一致性

**用户登录** (`POST /api/auth/login`) 仅使用 MySQL 的原因：

- **登录验证**: 只需验证用户名/邮箱和密码的正确性，这些核心认证信息都存储在 MySQL users 表中
- **性能优化**: 登录是高频操作，直接在 MySQL 中完成验证可以减少数据库访问次数，提高性能
- **数据一致性**: 认证信息集中在 MySQL 中，避免跨数据库查询带来的复杂性和性能开销

这种设计确保了系统在保持数据一致性的同时，充分利用了不同类型数据库的优势。

## 认证相关接口

### 1. 用户注册
- **接口**: `POST /api/auth/register`
- **描述**: 用户注册
- **数据库**: MySQL (users 表) + MongoDB (users 集合)
- **请求参数**:
  ```json
  {
    "username": "用户名",
    "email": "邮箱",
    "password": "密码"
  }
  ```
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "token": "JWT令牌",
      "user": {
        "id": 1,
        "username": "用户名",
        "email": "邮箱"
      }
    },
    "msg": "注册成功"
  }
  ```

### 2. 用户登录
- **接口**: `POST /api/auth/login`
- **描述**: 用户登录
- **数据库**: MySQL (users 表)
- **请求参数**:
  ```json
  {
    "loginId": "用户名或邮箱",
    "password": "密码"
  }
  ```
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "token": "JWT令牌",
      "user": {
        "id": 1,
        "username": "用户名",
        "email": "邮箱"
      }
    },
    "msg": "登录成功"
  }
  ```

### 3. 获取当前用户信息
- **接口**: `GET /api/auth/me`
- **描述**: 获取当前登录用户信息
- **数据库**: MongoDB (users 集合)
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "user": {
        "id": 1,
        "username": "用户名",
        "email": "邮箱",
        "avatar": "头像URL",
        "bio": "个人简介",
        "location": "所在地",
        "website": "网站",
        "skills": ["技能1", "技能2"],
        "experience": [...],
        "education": [...],
        "socialLinks": {...},
        "privacySettings": {...}
      }
    },
    "msg": "获取成功"
  }
  ```

### 4. 刷新令牌
- **接口**: `POST /api/auth/refresh`
- **描述**: 刷新访问令牌
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "token": "新JWT令牌",
      "user": {...}
    },
    "msg": "刷新成功"
  }
  ```

## 用户相关接口

### 5. 获取用户公开信息
- **接口**: `GET /api/users/:username`
- **描述**: 获取指定用户的公开信息
- **数据库**: MongoDB (users, profiles 集合)
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "user": {
        "username": "用户名",
        "avatar": "头像URL",
        "bio": "个人简介",
        "location": "所在地",
        "website": "网站",
        "skills": ["技能1", "技能2"],
        "socialLinks": {...},
        "isActive": true
      },
      "stats": {
        "followers": 10,
        "files": 5,
        "views": 100
      }
    },
    "msg": "获取成功"
  }
  ```

### 6. 更新用户档案
- **接口**: `PUT /api/users/profile`
- **描述**: 更新用户档案信息
- **数据库**: MongoDB (users 集合)
- **请求参数**:
  ```json
  {
    "bio": "个人简介",
    "location": "所在地",
    "website": "网站",
    "skills": ["技能1", "技能2"]
  }
  ```
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "user": {...}
    },
    "msg": "档案更新成功"
  }
  ```

### 7. 上传用户头像
- **接口**: `POST /api/users/avatar`
- **描述**: 上传用户头像
- **数据库**: MongoDB (users 集合)
- **请求参数**:
  ```json
  {
    "avatar": "头像URL"
  }
  ```
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "avatar": "头像URL"
    },
    "msg": "头像更新成功"
  }
  ```

### 8. 获取用户文件列表
- **接口**: `GET /api/users/:username/files`
- **描述**: 获取指定用户的文件列表
- **数据库**: MongoDB (users, profiles 集合)
- **URL参数**:
  - `page`: 页码
  - `limit`: 每页数量
  - `category`: 分类
  - `visibility`: 可见性
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "files": [...],
      "pagination": {
        "page": 1,
        "limit": 10,
        "total": 5
      }
    },
    "msg": "获取成功"
  }
  ```

## 个人档案相关接口

### 9. 获取当前用户档案
- **接口**: `GET /api/profiles/my`
- **描述**: 获取当前用户的档案信息
- **数据库**: MongoDB (profiles 集合)
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "profile": {
        "userId": 1,
        "files": [...],
        "totalViews": 100,
        "totalDownloads": 50,
        "followerCount": 10
      }
    },
    "msg": "获取成功"
  }
  ```

### 10. 添加文件到档案
- **接口**: `POST /api/profiles/files`
- **描述**: 添加文件到用户档案
- **数据库**: MongoDB (profiles 集合)
- **请求参数**:
  ```json
  {
    "title": "文件标题",
    "description": "文件描述",
    "visibility": "public/followers/private",
    "tags": ["标签1", "标签2"],
    "filename": "文件名"
  }
  ```
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "file": {...}
    },
    "msg": "文件添加成功"
  }
  ```

### 11. 更新文件信息
- **接口**: `PUT /api/profiles/files/:fileId`
- **描述**: 更新文件信息
- **数据库**: MongoDB (profiles 集合)
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "file": {...}
    },
    "msg": "文件更新成功"
  }
  ```

### 12. 删除文件
- **接口**: `DELETE /api/profiles/files/:fileId`
- **描述**: 删除文件
- **数据库**: MongoDB (profiles 集合)
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {},
    "msg": "文件删除成功"
  }
  ```

## 社交相关接口

### 13. 关注用户
- **接口**: `POST /api/social/follow`
- **描述**: 关注其他用户
- **数据库**: MySQL (follows 表) + MongoDB (follows 集合)
- **请求参数**:
  ```json
  {
    "userId": "被关注用户ID"
  }
  ```
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {},
    "msg": "关注成功"
  }
  ```

### 14. 取消关注用户
- **接口**: `DELETE /api/social/follow/:userId`
- **描述**: 取消关注用户
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {},
    "msg": "取消关注成功"
  }
  ```

### 15. 获取用户粉丝列表
- **接口**: `GET /api/social/followers/:userId`
- **描述**: 获取指定用户的所有粉丝
- **数据库**: MongoDB (follows 集合)
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "followers": [...],
      "pagination": {...}
    },
    "msg": "获取成功"
  }
  ```

### 16. 获取用户关注列表
- **接口**: `GET /api/social/following/:userId`
- **描述**: 获取指定用户关注的用户列表
- **数据库**: MongoDB (follows 集合)
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "following": [...],
      "pagination": {...}
    },
    "msg": "获取成功"
  }
  ```

### 17. 点赞文件
- **接口**: `POST /api/social/like`
- **描述**: 点赞文件
- **数据库**: MongoDB (profiles 集合中的 files.likes)
- **请求参数**:
  ```json
  {
    "fileId": "文件ID"
  }
  ```
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {},
    "msg": "点赞成功"
  }
  ```

### 18. 取消点赞
- **接口**: `DELETE /api/social/like/:fileId`
- **描述**: 取消对文件的点赞
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {},
    "msg": "取消点赞成功"
  }
  ```

### 19. 发表评论
- **接口**: `POST /api/social/comment`
- **描述**: 对文件发表评论
- **数据库**: MongoDB (profiles 集合中的 files.comments)
- **请求参数**:
  ```json
  {
    "fileId": "文件ID",
    "content": "评论内容"
  }
  ```
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "comment": {...}
    },
    "msg": "评论成功"
  }
  ```

## 通知相关接口

### 20. 获取用户通知列表
- **接口**: `GET /api/notifications`
- **描述**: 获取当前用户的通知列表
- **数据库**: MongoDB (notifications 集合)
- **URL参数**:
  - `page`: 页码
  - `limit`: 每页数量
  - `unreadOnly`: 是否只查询未读
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "notifications": [...],
      "pagination": {...},
      "unreadCount": 5
    },
    "msg": "获取成功"
  }
  ```

### 21. 标记通知为已读
- **接口**: `POST /api/notifications/mark-read`
- **描述**: 标记通知为已读
- **请求参数**:
  ```json
  {
    "notificationIds": ["通知ID列表"],
    "markAll": false
  }
  ```
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {},
    "msg": "标记成功"
  }
  ```

### 22. 删除通知
- **接口**: `DELETE /api/notifications/:id`
- **描述**: 删除指定通知
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {},
    "msg": "通知删除成功"
  }
  ```

### 23. 获取未读通知数量
- **接口**: `GET /api/notifications/unread-count`
- **描述**: 获取当前用户的未读通知数量
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "count": 5
    },
    "msg": "获取成功"
  }
  ```

## 搜索相关接口

### 24. 搜索用户
- **接口**: `GET /api/search/users`
- **描述**: 搜索用户
- **数据库**: MongoDB (users 集合) 或 Elasticsearch
- **URL参数**:
  - `q`: 搜索关键词
  - `page`: 页码
  - `limit`: 每页数量
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "users": [...],
      "total": 10,
      "page": 1,
      "limit": 20
    },
    "msg": "搜索成功"
  }
  ```

### 25. 搜索文件
- **接口**: `GET /api/search/files`
- **描述**: 搜索文件
- **数据库**: MongoDB (profiles 集合) 或 Elasticsearch
- **URL参数**:
  - `q`: 搜索关键词
  - `category`: 文件分类
  - `page`: 页码
  - `limit`: 每页数量
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "files": [...],
      "total": 10,
      "page": 1,
      "limit": 20
    },
    "msg": "搜索成功"
  }
  ```

### 26. 获取搜索建议
- **接口**: `GET /api/search/suggestions`
- **描述**: 获取搜索建议
- **数据库**: MongoDB (users 集合) + Redis (缓存)
- **URL参数**:
  - `q`: 搜索关键词
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "suggestions": {
        "users": [...]
      }
    },
    "msg": "获取成功"
  }
  ```

## 文件相关接口

### 27. 下载文件
- **接口**: `GET /api/files/download/:fileId`
- **描述**: 下载文件
- **数据库**: MongoDB (profiles 集合)
- **返回**: 文件下载

### 28. 预览文件
- **接口**: `GET /api/files/preview/:fileId`
- **描述**: 预览文件
- **返回**: 文件内容

### 29. 删除文件
- **接口**: `DELETE /api/files/:fileId`
- **描述**: 删除文件
- **数据库**: MongoDB (profiles 集合) + 本地文件系统
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {},
    "msg": "文件删除成功"
  }
  ```

## 其他接口

### 30. 服务器健康检查
- **接口**: `GET /health`
- **描述**: 服务器健康状态检查
- **返回示例**:
  ```json
  {
    "code": 0,
    "data": {
      "status": "ok",
      "timestamp": "2023-01-01T00:00:00.000Z",
      "uptime": 3600
    },
    "msg": "服务正常"
  }
  ```